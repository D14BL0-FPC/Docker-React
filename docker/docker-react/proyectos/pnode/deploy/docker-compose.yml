services:
  web:
    build:
      context: ${CONTEXTO}
      dockerfile: ./dockerfiles/node/nodebase
    container_name: ${NOMBRE}
    ports:
      - "3000:3000"  
    env_file:
      - ./.env
    image: francisco/nodejs
    volumes:
      - ./logs:/root/logs      
    networks:
      practica:
        ipv4_address: ${IP}
networks:
  practica:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: ${SUBNET}